/**
 * 圖表相關的工具函數 (佔位符)
 *
 * 未來可在此處添加數據格式化、顏色計算等共用邏輯。
 */

/**
 * 格式化數據以符合特定圖表庫的格式需求。
 * @param data - 原始數據
 * @returns 格式化後的數據
 */
export const formatDataForChart = (data: any[]): any[] => {
  // 待辦：根據圖表庫需求實現數據轉換邏輯
  console.log('Formatting data for chart (not implemented):', data);
  return data;
};
